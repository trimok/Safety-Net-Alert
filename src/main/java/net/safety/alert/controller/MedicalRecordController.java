package net.safety.alert.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class MedicalRecordController {
	/*
	 * @Autowired IMedicalRecordService medicalRecordService;
	 * 
	 * @PostMapping("/medicalRecord") public MedicalRecord createMedicalRecord(@RequestBody MedicalRecord medicalRecord)
	 * { return medicalRecordService.createMedicalRecord(medicalRecord); }
	 * 
	 * @PutMapping("/medicalRecord") public MedicalRecord updateMedicalRecord(@RequestBody MedicalRecord medicalRecord)
	 * { return medicalRecordService.updateMedicalRecord(medicalRecord); }
	 * 
	 * @PatchMapping("/medicalRecord") public MedicalRecord patchMedicalRecord(@RequestBody MedicalRecord medicalRecord)
	 * { return medicalRecordService.patchMedicalRecord(medicalRecord); }
	 * 
	 * @DeleteMapping("/medicalRecord") public void deleteMedicalRecord(@RequestBody MedicalRecord medicalRecord) {
	 * medicalRecordService.deleteMedicalRecord(medicalRecord); }
	 */
}